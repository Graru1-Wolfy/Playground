# Dev signing key for sideload / GitHub release builds only — not for Play Store production.
# Copy to keystore.properties (already committed for this repo's dev releases).

storeFile=keystore/bounce-check-dev.jks
storePassword=bouncecheck-dev
keyAlias=bouncecheck-dev
keyPassword=bouncecheck-dev
