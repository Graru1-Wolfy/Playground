name: Deploy GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "apps/web/**"
      - "packages/engine-fast/**"
      - "packages/schema/**"
      - "scripts/prepare-web-data.sh"
      - "scripts/write-web-sample-data.py"
      - ".github/workflows/deploy-pages.yml"
      - "package.json"
      - "package-lock.json"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          npm ci
          pip install -e packages/tf2sim
          pip install -e packages/engine-sim

      - name: Restore generated data cache
        uses: actions/cache@v4
        with:
          path: data/generated
          key: bounce-data-000to099-v1

      - name: Generate missing bucket data if no cache
        run: |
          if [ ! -f data/generated/000to099/0.bin.gz ]; then
            echo "No cache — generating heights 0-99 (this may take a while on cold start)..."
            engine-sim --range 0-99 --workers 4
          fi
          ls -la data/generated/000to099/*.bin.gz 2>/dev/null | wc -l

      - name: Build web app
        run: |
          npm run build:packages
          npm run build:web
          bash scripts/prepare-web-data.sh apps/web/dist/data

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: apps/web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
